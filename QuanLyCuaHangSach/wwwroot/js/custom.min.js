function updateTime() {
    console.log("updateTime() được gọi nhưng chưa có nội dung.");
}

// Nếu trang web đang gọi `setInterval(updateTime, 1000);` thì khai báo trước
var timer = setInterval(updateTime, 1000);
